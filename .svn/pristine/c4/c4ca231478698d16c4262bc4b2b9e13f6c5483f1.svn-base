package com.sina.book.parser;

import com.sina.book.control.download.DownBookJob;

public interface IParserListener {
	/**
	 * 解析了某一章节内容时，立即回调
	 * 
	 * @param downBookJob
	 * @param chapterId
	 */
	public void theChapterHasBeenParsed(DownBookJob downBookJob, int chapterId);
}
